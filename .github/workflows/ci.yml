name: CI master

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checking out
      uses: actions/checkout@v4

    - name: Setting up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'zulu'
        cache: gradle

    - name: Building
      run: |
        chmod +x build.sh
        ./build.sh
      working-directory: tools

    - name: Upload Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: Application_Artifact
        path: ${{ github.workspace }}/Notes-App
        overwrite: true
        if-no-files-found: error

# Temprorary disabled
    # - name: Create Draft Release
    #   env:
    #     GH_TOKEN: ${{ github.token }}
    #     GH_REPO: ${{ github.repository }}
    #   run: gh release create Notes-App/mapping/release/usage.txt Notes-App/apk/release/app-release.apk Notes-App/bundle/release/app-release.aab Notes-App/reports/lint/lint-results-debug.html Notes-App/apk/debug/app-debug.apk --draft -n "Test release" -t "Test title"
